# The Pool Campaign, Stages P-5–P-7: Three Registered Kills and the Escalation

**Date:** 2026-08-13. All registrations pre-run; every gate scored as hand-declared; no rescues.
Registrations/scores (eng repo): P-5 `c3dff43/8225d5c` · P-6 `c811427/f775778` · P-7 `821e050/dfba0a9`.
Instruments: `mass/mass-poolP{5,6,7}{.py,-run.txt}`. Prior stages: P-1–P-4 (see the swing-44
addendum and `MASS-LEDGER-SWING34-41.md`).

## What was asked

P-4 left two owed items: the light edge's saturation clause (Q1 FAIL +21.3%, the intercept fact
n_f → A) and the large-A asymptote (not extractable from tangential-frozen droplets). P-6's honest
geometry then re-opened a third: the plateau count itself.

## The three stages

**P-5 — the lock-surcharge census (structure-side saturation): KILLED, both pre-named directions.**
Participation = seat pool + interior pairs hosting by opening a *non-critical* lock at the ledger's
own price DP/2 = 7.01 within the credit 9.694 (criticality exact, double-cover flow oracle).
Fidelity to P-4 was perfect (φ_W to 0.001 — deterministic chassis). The channel starves the light
edge (+0.085 where +0.29 was needed) and floods the plateau (drift 0.191 ≫ 0.05). The teaching:
N_open/A *grows* with A — small-droplet locks are load-bearing (A = 30: essentially all critical),
bulk matchings carry abundant alternating slack. Saturation is not structural.

**P-6 — the full-anneal instrument: the geometry cured, the window retired, the halo found.**
1500 free-displacement sweeps after the frozen stage. ★ z_bulk(500) = 4.873 vs the capacity seam's
*derived* realized count 4.846 (+0.6%) — an independent cross-front confirmation (swing 41). The
frozen window match at A = 220 carried artifact share −0.165 (0.342 → 0.177 against measured
0.331) — retired per the replacement policy. The light edge moves *away* from saturation under
honest annealing (anneal-fidelity owner eliminated). φ_∞ failed as registered under the retained
Fermi map (fit −0.10). The freed ledger builds core + diffuse halo (its own surface far wider than
the imported 0.574 fm tail) — instrument finding, named.

**P-7 — the native depth map (credit-side, the named decider): the escalation fires.**
The ledger's own energy as the depth: u = clip(1 − E_i/E_bulk, 0, 1), node_E being the energy the
anneal minimizes; cost law and credit unchanged. Config identity to P-6 exact. The annealed energy
distribution is *narrow* (E_bulk = −17.8 MeV, stable across every size ≥ 107 — measured, no claim
attached), so the credit's u\* = 0.7195 excludes only the outer few percent: the plateau floods
(φ_N(220) = 0.689, dead-pole kill) and the light edge overshoots (φ_N(55) = 1.218, φ_N(30) = 1.644
— *seats exceed the census*). The last standing owner dies; the pre-named escalation fires.

## State of the front after P-1…P-7

- **Stand untouched:** the form THEOREM (no bulk-column route to A^{−1/2}; the gap is count
  dilution Δ = δ_cell/√n_f) and the derived DEPTH (δ_cell = 6.51, +7.4% vs the light-edge
  intercept).
- **The plateau count is open with five dead zero-dial readings bracketing the measurement from
  both sides:** 0.210 (open sites) · 0.99 (diffuse patches) · lock-surcharge (starves/floods) ·
  Fermi-map-on-honest-geometry (→ 0) · energy-map-on-honest-geometry (0.69). The frozen-map Q2–Q4
  curve match of P-4 is quantified as living in the frozen artifact.
- **The light edge is what the geometry licenses:** below A ≈ 107 the annealed census offers more
  seats than quanta (φ_N ≥ 1), so whole-census participation — the measured intercept fact — costs
  nothing to admit. The census-cap reading (min(φ, 1) → 1.0 at the intercept vs measured ~1, 0.90
  at Q1) is noted, unclaimed: it was not registered, and it does nothing for the plateau.
- **The named open:** what sets the scale-free ≈ 1/3 participation of the bulk census. The
  adjacent clean fraction 1/3 remains **inoculated** (swing-34 clause) until a mechanism *forces*
  it. Escalation objects for a future registration, in declared order of preference: (a) the
  participation is not a static geometric census — a credit-limited count at the *gap* level (the
  borrow dynamics set n_f; geometry only hosts it); (b) a two-regime form (light edge and plateau
  as different physics with a derived crossover); (c) the census cap as the light-edge clause with
  a separate plateau owner.

## Disposition (2026-08-13, judgment call under delegated authority)

The front **parks here**. Five registered kills bracketing the measurement is a rigorous null that
names its missing piece precisely; a sixth mechanism hunted in the same evening would be
dial-chasing, not derivation. The pool item stays open on the exhibit with the full record beside
it. The campaign resumes when one of the escalation objects can be *registered from standing
constants* rather than proposed from hope.
