# Mass Ledger — Swing 8: Mirror Unification, and the Crown Contracted to One Number
> **SUPERSEDED IN PART — corrected 2026-09-08.** The corrected localization bound destroys the
> flat cluster ladder that this run uses to contract the crown. The \(\gamma\), capacity, crown,
> loop-credit, and dependent skin/displacement chain are **not current results**. Historical values
> and verdicts remain verbatim; see the
> [canonical correction](../MASS-LEDGER-CORRECTION-2026-09-08.md).

**Follows:** swings 1–7. Registration (ledger d942dc7) committed BEFORE the run.
GROUNDING gate binding; labels tagged; imports named; fitting is the only sin.
Instrument: mass-swing8.py / mass-swing8-run.txt.

## 1. R8a — the mirror pair UNIFIES (an import artifact caught and killed)
Swing 6 compared radius CONVENTIONS, not radii: its h band (1.75–1.82) was naive
charge-subtraction, its t band (1.54–1.68) point-proton-flavored. Declared
unfolding (r_p = 0.8409 fm, r_n² = −0.1155 fm²; r_c(³He) = 1.9506 muonic,
r_c(³H) = 1.755) gives point-proton radii 1.776 / 1.614 — and the isospin-even
MATTER operator forces near-equal matter radii: mirror-combined 1.724 fm both
(Coulomb split 1–2%). On the common band (1.65–1.75):
    δ₀(h) = 15.19,  δ₀(t) = 15.10  —  |split| = **0.09 MeV**  (was 2.74).  PASS.
**Bonus consistency (not registered, arithmetic):** the mirror binding difference
B(t) − B(h) = 0.764 MeV is carried almost exactly by the ledger's strain entry
(1.00 MeV at the common radius) — the corrected mirror pair verifies the strain
column at A = 3 with 0.09 MeV residual. ("Charge independence" bookkeeping,
checked at the lightest strained pair.)

## 2. R8b — consequence for swing 6, on the record
T-C6b(ii) ("mirror split δ₀(t) > δ₀(h)") PASSED as registered — and is now
**DEMOTED to import artifact**: the split was two conventions fighting, not
physics. The registered pass rode a bad import band; booked additively in the
swing-6 doc. Unaffected and re-verified on the corrected ladder: T-C6b(i)
α−d gap = +3.39 ≥ 1.5 PASS; monotone d ≤ z2-mean ≤ α PASS.

## 3. R8c/R8d — the corrected ladder and its shape
    z:      1        2 (h/t)     3 (α)      4.78 (frozen books)
    δ₀:   14.31      15.14       17.70      22.2  [20.0, 24.9]
    step:      +0.84       +2.56       +2.54 per partner
- R8c convexity PASS (0.84 < 2.56).
- R8d the pure-quadratic form (zero linear term) FAILS the frozen endpoint as
  registered (26.5 ∉ [20.0, 24.9]) — rejected.
- **The signature (the swing's sharpest fact):** the measured step 2→3 (+2.56)
  equals the slope required to reach the frozen endpoint from the alpha
  (+2.54/partner) to better than 1%. The corrected ladder + bulk books are
  consistent with **constant marginal lock-deepening w ≈ 2.55 MeV per partner
  for z ≥ 2**, with only the 1→2 step anomalous (+0.84) — equivalently: the
  law is linear in z from z = 2 through capacity, and the lone pair sits
  +1.7 MeV ABOVE that line's z=1 extrapolation.

## 4. R8e — dwell/universal-contact class EXCLUDED (with a self-catch booked)
With the honest tail normalization (A_S = 0.8846 fm^{-1/2} import), the deuteron
interior fraction at lock reach 1.9–2.4 fm is P_in = 0.30–0.44 ⇒ implied contact
constant 32–48 MeV: outside the frozen band AND non-universal across the ladder.
The "universal contact constant × dwell dilution" class is EXCLUDED.
**Self-catch, on the record:** the parent's first pass used the pure-tail formula
P_in = 1 − e^{−2κc} = 0.63 — a normalization inversion that produced a spurious
dead-on hit (22.8 vs 22.2). Caught before booking by recomputing with the
measured asymptotic normalization. Same reflex the swing-7 slope bend exposed;
same discipline caught it.

## 5. The crown, contracted
Not taken — narrowed. Excluded to date: pinned-share ∝ z; geometric compounding;
half-leakage (η = ½); linear-in-f generally (no forced coefficient); pure
quadratic; dwell/universal-contact. Surviving qualitative candidate:
**loop-closure / patch-rigidity coherence** — the second-and-later partners close
circuits through existing locks (in the complete-graph clusters, triangles per
bond = z − 2 exactly: 0, 1, 2 for d, t/h, α — the cheap-first-step signature in
the right place), each closure deepening every lock by a constant increment.
**The crown is now ONE NUMBER: derive w = 2.55(20) MeV per partner** (and the
+0.84 first-step anomaly) from winding bookkeeping. For scale, named without
claim: w sits at (1−γ)·δ₀-class and τ_b/8-class magnitudes — no identification
made; deriving w forces the whole curve, the frozen endpoint, and the ×1.55.

## Swing-8 scorecard
| item | status |
|---|---|
| R8a mirror unification | PASS — split 0.09 MeV (was 2.74); strain entry verified at A=3 |
| R8b T-C6b(ii) | DEMOTED (import artifact); gap/monotone tests re-verified |
| R8c convexity | PASS on corrected ladder |
| R8d pure quadratic | REJECTED at frozen endpoint, as registered |
| R8e dwell class | EXCLUDED (honest normalization); inversion self-catch booked |
| constant-marginal signature | +2.56 vs +2.54/partner (1%) — any native law must produce it |
| crown | OPEN, contracted to one number: w = 2.55(20) MeV/partner |
