# Induction-Kernel Foundation — source-grounded setup for the α/θ totality-closure run
Assembled 2026-07-05 (Parzival, step 1). Purpose: ground the kernel in Pope-Osborne's own text so the two-model run builds on the corpus, not invention (anti-Wyler). Feeds ALPHA-kernel-{opus,fable}.txt.

## SOURCE GROUNDING (verbatim / located)
1. **Mach's Principle — POAMS adopts it explicitly.** *Light-Speed, Gravitation & Quantum Instantaneity* (osborne-2007) ch01-02 (~line 122): "it is this holistic system, through its masses, distributions and motions, which determines, both locally and non-locally, an inertial frame. This view is essentially Mach's Principle." → inertia (= mass = rate of angular change, L=dS/dθ) is set by the TOTALITY, non-locally.
2. **The correlation is INSTANTANEOUS, not a travelling signal.** Same book ch01-02 (~lines 224-226): energy transfer / quantum interaction is "direct and therefore instantaneous," with "foreknowledge of the conditions for their reception at the far end." Pioneer-Anomaly paper (~line 296): all bodies "correlated and holistically interconnected by the law of the conservation of angular momentum." → the kernel is an instantaneous holistic AM-correlation, i.e. a POAMS Green's function over the totality, NOT propagation. (Book title itself: "Quantum Instantaneity"; 2005 book: "Immediate Distant Action and Correlation.")
3. **α is the TILT/PITCH angle** (prior runs alpha-cone-{opus,fable}.txt): v/c = **sin θ**, θ = tilt of the circulation's resultant time-vector off the proper-time axis on the Pythagorean cone.
4. **PROVEN LOCAL NO-GO** (alpha-cone runs — the load-bearing lemma): the isolated Pythagorean cone is SCALE-INVARIANT; the right-triangle similarity group acts transitively on θ∈(0,π/2); closure ∮dφ=2πn + ħ-quantization break the SCALE part but COMMUTE with the tilt → "no combination of closure + quantization + Pythagoras selects θ." So θ is provably unfixable by ANY single-vortex/isolated-cone argument. (= today's single-vortex NULL, same wall.)

## THE LOGICAL FORCING (why the kernel is the only move left)
Isolated cone can't fix θ (scale-invariance, PROVEN). Mach's Principle says θ is NOT set locally — it's set by the totality, instantaneously, via AM conservation. THEREFORE: the induction kernel = the totality-coupling that BREAKS the isolated cone's scale-invariance by referencing the vortex's tilt to the whole. That broken symmetry is what fixes θ (hence α). This is forced, not chosen.

## PRE-REGISTERED KERNEL MECHANISM (to derive/test — target hidden, null allowed)
Candidate (from MEMORY.md α-frontier + the 4π in today's N=W/4π): the vortex couples to the totality through the **solid angle** its tilted circulation subtends against the whole sky of distant matter. A tilt-dependent solid angle (e.g. 2π(1−cosθ)) breaks homogeneity → a fixed-point/eigenvalue condition, schematically **cos θ = 1/C** (C = totality coupling = induction eigenvalue), giving small θ (hence small α) as the near-critical signature C→1. Today's run fixes the CROSS-CHECK: the kernel must deliver the closure weight **W ≈ 4.72×10⁵** (N = W/4π ≈ 37,558, α = √(2/N)); local routes give W~10³, i.e. the kernel must supply **Δln W ≈ 4–5** (~e^4.5 ≈ 90×). The 4π in N=W/4π = the totality's full solid angle — consistent with a solid-angle kernel.
- Models MAY reject the solid-angle candidate and propose the correct POAMS kernel form — but must PRE-REGISTER it (equation before numbers) and derive it from Mach's Principle + instantaneous AM-correlation, not fit 137/274.
- Expected: TRANSCENDENTAL (integer/clean-π excluded; 137.036 not an integer; Wyler = cautionary numerology).

## SUCCESS / NULL CRITERIA
- FORCES θ: kernel's fixed point gives a closed (transcendental) expression for θ → compute α, Python-verify vs 137.035999 AFTER (never before). If it lands, that's the crack.
- PARTIAL: kernel reduces α to exactly ONE totality constant (e.g. C or an effective mode-count) → we've proven α = that one Machian number and named it precisely (publishable structural result).
- NULL: even the totality coupling leaves θ free → α is a free constant of the universe, full stop.

## SOURCES FOR THE MODELS TO MINE
osborne-2007/chapters/ch01-02.md (Mach's Principle, instantaneity) + gravitation/instantaneity chapters ch05-08 (quantitative inertia/totality coupling if present); poams-books/2015-einstein-lost-legacy.txt (App 3-5); alpha-cone-{opus,fable}.txt (proven no-go + α=sinθ); memory/poams-audit/ALPHA-rho-vortex-{opus,fable}.txt (α=√(2/N), N=W/4π, Δln W≈4.5); CHARTER.md (liturgy).
