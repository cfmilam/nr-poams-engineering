# Mass Ledger — Swings 32–33: The Credit Transfer and the Ambush Confrontation

**Date:** 2026-08-12 (early morning) · **Registrations:** 72428eb (32), 5fa1759 (33) — both pre-run.
**Scores:** 2762f3f (32), 1cdc844 (33). Instruments: `mass/mass-swing{32,33}{.py,-run.txt}`.
**Inherited state (swings 30–31):** γ pinned conditionally at γ* = 0.824(7); at γ* the split
residual is real (~10%: step1 0.850 vs h = 0.9458 hot, step2 2.465 vs c₃ = 2.3363 cold, sum −1.0%)
with one named shape candidate; the pairing item open with a four-target ambush (T1–T4) and an
inoculation list of seen-adjacent coincidences.

---

## 1. Swing 32 — credit transfer through the shared bond

**Mechanism (derivation-first).** A swept geometric phase banked on a shared turn counts only
against a counter-circulation (the relational cash rule). The closing bond's swept cone at the
first closure contains a component whose reference is the *not-yet-existing* second closure
through that bond: latent at z = 2, cashing when the second closure locks — into the second
closure's step. Booked form: step1 = h(1−f), step2 = c₃ + f·h. **Sum-preserving by construction**
(measured sum residual −1.0% is the mechanism's signature class). Deuteron, alpha total, bulk
books/crown, and the two-cell sign gates are all invariant; the skin rider (surface bonds hold
uncashed latents) stays named-unclaimed.

**The frozen fork** (eight fractions, hand arithmetic in the registration; gates: γ*-pinned bands
step1 [0.75, 0.95], step2 [2.35, 2.58]; sum within 3%; strict ≥10%-relative improvement on both
residuals — margin pre-set so the mirror-image row dies by rule, not rounding):

| row | f | h′ | c₃′ | verdict |
|---|---|---|---|---|
| F1 vertex excess | 1/3 | 0.631 | 2.652 | dead (bands) |
| F2 even per-bond | 1/2 | 0.473 | 2.809 | dead (bands) |
| F3 re-suppression | 2/z_c | 0.563 | 2.719 | dead (bands) |
| F4 projector | cos²θ_w | 0.611 | 2.671 | dead (bands; projector ≠ phase) |
| F5 patch budget/end | 1/z_c | 0.754 | 2.528 | dead (improvement margin: 11.26% vs 10.17%) |
| **F6 half closing-vertex** | **1/6** | **0.788** | **2.494** | **survives (−7.3% / +1.2%)** |
| F7 second sweep | (2/z_c)² | 0.791 | 2.491 | survives (numerically degenerate with F6; z_c ≈ 2√6 flagged, no meaning claimed) |
| F8 patch budget/turn | 1/(2z_c) | 0.850 | 2.432 | survives (+0.04% / −1.3%) |

**Verdict — as registered.** The *mechanism* is established at gate grade: a sum-preserving
transfer is the only move that reduces both residuals at fixed sum, and the entire ≥1/5 fraction
class is excluded. The *fraction* is **identification grade**: F6 = 1/6 is the derived central
(per-vertex spherical excess Ω_w/3 × the ledger's two-claimant ½ — the δ_pair booking rule applied
at the closing vertex), but its joints — closing-vertex uniqueness vs T = 0 mutuality (the R1
tension) and the ½ extended from turns to vertex excesses — remain precedent-shaped, so the grade
caps as declared. F8 is the data-centered unforced alternative; its step2 residual sign is
opposite F6's (a future discriminator lever).

**Diagnostics (unclaimed).** Re-triangulation at every survivor: γ_t, γ_h rise toward γ_α;
**spread 0.0142 → 0.0079 — the transfer explains half the triangulation spread.** The residual
0.0079 is the t–h gap: transfer-invariant, strain-side, same mirror/displacement family as T-P3′.
Fixed-point note: re-extracting at γ*′ chases the transfer; the extraction and the credits are not
yet a self-consistent pair (frozen-γ* scoring stands; convergence question booked open).

## 2. Swing 33 — the pairing candidate walks into the ambush

**Mechanism (zero pairing-data inputs).** The odd census holds one unclosable half-turn (ħ/2
without a counter-holder). The gap = the re-pairing credit the odd quantum cannot cash directly;
the only route is **borrow-exchange through an existing pair** — two channel vertices ⇒ the credit
carries γ². That two-vertex argument was filed as the independent forcing derivation the
swing-31 inoculation clause demands, *before* any further data look. The pool dilution P(A) stays
underived — read from the frozen T1 record — so the **absolute median was ungated by
construction** (γ*²δ_pair = 9.523 report-only).

**Scores (1973 gaps: 1008 n, 965 p):**

- **S33a PASS — the scale hit.** Predicted n/p split = k/(2r_q) × P = 0.8372 × 0.915 = 0.766;
  measured 10.05 − 9.27 = **0.781 (−2.0%)**. Zero dials, uninoculated: *the n/p pairing split is
  the strain entry at the pair's own separation.*
- **S33b FAIL as registered — the co-drift shape.** Quartile splits {0.541, 0.927, 0.298, 1.263}
  vs predicted {0.544, 0.752, 0.834, 0.837}: Q1 lands at 0.6% (noted), Q2/Q4 inside the ±60%
  gate, **Q3 (A 129–170, the deformation onset region) collapses to 0.298 — the gate fails.**
  No rescue. The split's A-structure is not the simple shared-pool track; Q3/Q4 join the target
  set as **T5** = {0.541, 0.927, 0.298, 1.263}.
- **S33c PASS (sign).** Frontier suppression dist-1 8.33 < dist-3+ 10.05 (−17%); n-only −25%,
  p-only −13% now on record.
- **S33d PASS — the census identity.** median[δV_pn·A/(2(1−y²))] = **21.83** ∈ [19.6, 29.4]
  (463 ee nuclides, median A 132): the mid-shell quarter double-difference reads the ledger's own
  derived a_sym at −10.9%. **The T4 target is census bookkeeping, not a pairing object**; the
  −11% residual = the direct np frontier credit + estimator structure, named.

**Verdict — grade cap honored.** Structure grade + two uninoculated quantitative hits (S33a,
S33d). The pairing item **stays open**: the pool census / A^{-1/2} mechanism is still the one
underived object, now confronted by T1–T5. The ambush held — no banned form entered ungated.

## 3. State of the front after the morning

| item | state |
|---|---|
| split residual (h hot / c₃ cold) | mechanism established (sum-preserving transfer); fraction identified [1/6 · (2/z_c)² · 1/(2z_c)], derived central 1/6 |
| γ triangulation spread | half explained by the transfer; residual 0.0079 = t–h strain gap (T-P3′ family) |
| pairing n/p split | **= k/(2r_q), −2.0%** — new permanent-record identity |
| δV_pn (mid-shell) | **= (2a_sym/A)(1−y²), −10.9%** — explained as census curvature |
| pairing magnitude/shape | open; targets now T1–T5; pool census underived (shared with A^{-1/2} item) |
| exhibit | §9 update PROPOSED (ratification required): transfer mechanism + the two new identities + T5 |
