# The Mass Ledger — Five Entries and the Forced Peak (Swing 1)
**Front:** Mass as trapped rate → binding as packing discount.
**Registration:** M-C1 (2026-08-11 01:25, ledger 89d309e) — committed BEFORE this derivation.
**Rule:** structure and coefficient ratios must be FORCED by ledger bookkeeping;
every free constant is a NAMED IMPORT; fitting is the only sin. Extant shadow:
von Weizsäcker 1935 (semi-empirical mass formula) — cited as comparison class;
the claim here is the bookkeeping derivation, not the form's novelty.
GROUNDING gate: extant-physics terms appear only as tagged comparative labels.

## Setup
A = circulation quanta packed at dense contact (comparative label: nucleons);
Z = census count of the sense class with the external phase handle (label: proton
class); N = A − Z. Dense contact of equal quanta ⇒ fixed number density
(volume ∝ A, radius R = r₀A^{1/3}) — bookkeeping, not import; r₀ (the size unit)
IS a named import. Binding B = total trapped-rate discount; b = B/A.

## P1 — Bulk (volume entry): FORCED
Finite coherence range ξ ≈ contact range (the registered LOCAL regime — the
opposite pole to the atom's GLOBAL census vote on the Aufbau front). Discount
accrues per CONTACT, δ per locked pair. Dense equal packing: coordination z = 12
(densest equal-sphere packing — geometry). Interior-dominated:
    B_bulk = (z/2)·δ_eff·A  ⇒  a_v = (z/2)δ_eff.
Extensivity is the theorem here: finite range ⇒ ∝A (global locking would give ∝A²).
**Forced: shape ∝A, sign, and the reading of a_v as contact bookkeeping.**

## P2 — Boundary (surface entry): FORCED, ratio included
Bond-deficit integral for a sharp fixed-density sphere with isotropic contacts at
range 2r₀: a quantum at depth h < 2r₀ loses the spherical-cap fraction
f(h) = (1 − h/2r₀)/2 of its contacts; ∫₀^{2r₀} f dh = r₀/2. Missing bonds =
(1/2)zρ·4πR²·(r₀/2) = (3/4)zA^{2/3}. Deficit = δ·(3/4)z·A^{2/3}, i.e.
    **a_s/a_v = 3/2 — forced (sharp-contact limit), no dial.**
Named idealization: the sharp surface. A diffuse boundary can only LOWER a_s/a_v;
the classical fitted shadow (≈17.8/15.75 ≈ 1.13) sits below 3/2 by ~25% —
booked as the diffuseness residual, a swing-2 derivation target (no credit taken).

## P3 — Census imbalance: FORCED (shape, sign, and a second root)
Two independent forcings, one answer:
(i) LEDGER: same-sense double-filing forbidden (P-I4/Schur, reused from the Aufbau
front) ⇒ two filing ladders; moving k = (N−Z)/2 quanta up one ladder costs
quadratically; extensive ladder density ⇒ cost ∝ (N−Z)²/A.
(ii) GEARS: two touching circulations mesh only when counter-rotating (contact
points co-move); co-rotating contact grinds (phase clash) ⇒ δ_unlike > δ_like,
i.e. γ ≡ δ_ll/δ_ul ∈ (0,1) — SIGN FORCED. Sense-relabel symmetry (u → −u,
u = (N−Z)/A) forces the mix cost even in u; smooth + minimum at balance ⇒ leading
term u²·A = (N−Z)²/A regardless of packing detail.
**Forced: scaling (N−Z)²/A, sign (balanced favored), and the gear inequality.**
Magnitude (a_sym) = ladder-top rate and Δδ = (1−γ)δ_ul: named imports at this swing.
Immediate qualitative hit, no dial: an isolated LIKE-sense pair barely locks ⇒
no bound like-pair dimer, while the unlike pair binds (comparative labels: nn, pp
unbound; np = deuteron bound) — the ledger's own sign showing in the lightest data.

## P4 — Pairing: ladder FORCED, exponent NOT (named)
Within one sense class, reversed-orientation quanta mesh pairwise (the same
doubling-by-two bookkeeping derived on the atom front). Even count ⇒ all paired;
odd ⇒ one unpaired at the ladder top. Hence the mass ladder
    even-even < even-odd < odd-odd  — FORCED (order and signs).
The magnitude class A^{-1/2} is registered as an EMPIRICAL CLASS (the classical
shadow fits it too); swing-1 takes no credit. Candidate route (swing 2): top-shell
co-location × coherence volume. Odd-odd cross-class top pairing back-credit
(lightest self-conjugate cases) — named, not derived today.

## P5 — Phase-sense strain (label: "Coulomb"): FORCED shape + geometric 3/5
The Z-class carries the long-range phase handle (charge dissolved: a sense class
with external phase gradient, not a substance). Pair strain at range falls as 1/r —
THE NAMED IMPORT (the same Newtonian-shape import the n-body charter names at
every use). Uniform fixed-density sphere (forced) ⇒ pairwise sum = (3/5)·k·/R per
pair ledger with no self-pairing ⇒
    E_strain = (3/5)·k·Z(Z−1)/(r₀A^{1/3});   a_c = (3/5)k/r₀.
Geometric 3/5 forced; Z(Z−1) (not Z²) forced by pair bookkeeping; k (strain
quantum; label e²/4πε₀ = 1.44 MeV·fm) and r₀ = 1.2 fm are measured imports OUTSIDE
the binding curve ⇒ a_c = 0.72 MeV with no curve contact.

## The Forced-Peak Theorem
Along the balanced valley (leading class), per-quantum discount
    b(A) = a_v − a_s A^{−1/3} − c A^{2/3},  c = a_c/4 (naive valley Z ≈ A/2).
THEOREM. If a_s > 0 and c > 0 — both signs FORCED (P2 missing contacts; P5
like-sense strain) — then b(A) has exactly one interior maximum:
    b′ = (a_s/3)A^{−4/3} − (2c/3)A^{−1/3} = 0  ⇒  A* = a_s/(2c) = 2a_s/a_c = 3a_v/a_c,
    b″(A*) = −(2c/3)A*^{−4/3} < 0.
Existence and uniqueness are theorem-grade; the census bend (valley Z*(A) < A/2)
only shifts A* RIGHT (strain growth softened). **The peak of the discount curve is
forced by bookkeeping — mid-mass quanta are the most discounted, ends of the
table less so. No tuning enters: A* is a coefficient ratio.**

## Location under named imports (registered class A ∈ [40, 80])
Scale datum candidates for δ_ul (per unlike contact), both named, neither chosen
post-hoc:
- FREE-PAIR datum (deuteron binding): δ_ul = 2.22 MeV. Max-discount packing is the
  best 2-coloring of the z=12 contact graph (8 unlike + 4 like — the graph is not
  bipartite): a_v = (8δ_ul + 4γδ_ul)/2 = (4+2γ)·2.22 ⇒ with the forced a_s = (3/2)a_v:
      A* = 3a_v/a_c = 9.25·(4+2γ) ∈ [37, 56] for γ ∈ (0,1), census bend pushing right
  ⇒ lands in the registered class for γ ≳ 0.16. Ratio check: a_s/a_c ∈ [18.5, 27.8]
  (classical fitted shadow ≈ 25 — inside, no dial).
- PACKED datum (alpha, 4 quanta/tetrahedron, 6 contacts: 4 unlike + 2 like):
  (4+2γ)δ_ul = 28.3 MeV ⇒ δ_ul ≈ 5.7 MeV at γ = 1/2 — 2.5× the free-pair value ⇒
  A* ≈ 118, OVERSHOOTS the class.
**Finding (booked, not smoothed):** the per-contact discount is not a free-pair
observable — the free pair is diffuse (mostly outside contact range: the same
diffuseness that softens 3/2), the packed cluster is confined. The two data brackets
the in-bulk scale and their 2.5× tension is swing 2's first derivation target
(packing-confined pair discount). The theorem (existence, uniqueness, A* = 3a_v/a_c)
and every forced shape/sign/ratio above stand independent of the scale datum.
Wyler gate honored: nothing was tuned toward A = 56; the class statement rests on
named imports only.

## Scorecard (swing 1)
| entry | forced | named import / open |
|---|---|---|
| P1 volume ∝A | shape, sign, extensivity theorem | scale δ_eff |
| P2 surface A^{2/3} | shape, sign, **a_s/a_v = 3/2** (sharp) | diffuseness residual (~25%) |
| P3 census (N−Z)²/A | shape (two roots), sign, gear inequality | a_sym magnitude; γ value |
| P4 pairing ladder | ee<eo<oo order + signs | A^{-1/2} exponent (empirical class) |
| P5 strain Z(Z−1)/A^{1/3} | shape, 3/5, pair count; a_c = 0.72 (curve-free imports) | 1/r range law (named), k, r₀ |
| PEAK | existence + uniqueness + A* = 3a_v/a_c; class hit under free-pair scale | in-bulk scale (2.5× tension, swing 2) |
