# Mass Ledger — Swing 3: The Phase-Capacity Law and the Forced Peak, Located
**Follows:** swing 1 (56b6f16), swing 2 (43fb9a7, 71d6382, 44656c2). GROUNDING gate
binding; labels tagged; imports named; fitting is the only sin.

## 1. The data that force a capacity
Per-bond NET discounts of the lightest clusters spread 2.1× (2.22 dimer, 2.83
triton, 4.72 alpha) — but per-bond GROSS (net + localization tax, the swing-2
column) flattens. Tax floor per quantum: 3D uncertainty bound ⟨p²⟩⟨r²⟩ ≥ (9/4)ħ²
⇒ τ = (9/8)(ħ²/m)(1−1/n)/⟨r²⟩, matter radii from measured charge radii minus the
Z-class quantum's own (imports named: r_d 1.955, r_t ~1.54–1.68, r_α 1.452 fm;
ħ²/m = 41.47 MeV·fm²):
    per-bond gross: dimer 14.4 | triton 13.9–15.9 | alpha 15.8  →  δ₀ = 15.0 ± 1.0
Flat within ±7% across coordination z = 1, 2, 3 while nets spread 2.1×: the gross
contact discount is a CONSTANT per bond in the unsaturated regime. (Two-column
ledger verified a third time, now at three points.)
Bulk: per-bond gross = (a_v + τ_b)/6 = (15.75 + 20.1)/6 = 5.97 MeV — 2.5× BELOW δ₀.
Unsaturated fcc would give a_v = (12/2)·15.0 − 20.1 ≈ 70 MeV/quantum — 4.5× the
observed 15.75. **Saturation is not optional; the data force it at 4.5×.**

## 2. The capacity law (patch saturation, derived form)
POAMS mechanism: a mesh is a phase-lock over a contact PATCH — a cap of solid
angle Ω_w on the quantum's circulation sphere (patch half-angle θ_w set by the
lock range: surfaces within coherence gap ⇒ finite cap). The sphere is a finite
resource: total lockable solid angle = 4π. z patches saturate when zΩ_w > 4π:
    δ_eff(z) = δ₀ · min(1, z_c/z),   z_c = 4π/Ω_w
    per-quantum gross g(z) = (z/2)δ_eff = (z/2)δ₀  [z ≤ z_c]  |  (z_c/2)δ₀ = C  [z > z_c]
— per-quantum gross CAPS at C. This is "saturation of nuclear forces" (comparative
label) as solid-angle bookkeeping: partners beyond capacity share arc, adding nothing.
**Extraction (ledger, not fit):** z_c = 12·5.97/δ₀ = 4.78 ± 0.35 ⇒ Ω_w = 2.63 sr,
θ_w = 54.5° ⇒ cap C = (z_c/2)δ₀ = 35.9 MeV/quantum.
Consistency with no dial: alpha (z = 3) and triton (z = 2) sit BELOW z_c ⇒ predicted
full-strength δ₀ — observed (that is §1's flatness, now explained, not just noted).
Derivation of θ_w itself (from lock range ~ the quantum's intrinsic rate scale
λ̄ = ħ/mc: parabolic-gap estimate gives ξ ≈ 0.4–0.5 fm ≈ 2λ̄, z_c ≈ 5.0–5.3,
consistent) — booked CONSISTENT, coefficient OPEN (named; no Wyler credit).

## 3. Surface reattribution theorem (P2 revised — additively, swing-1 text stands)
THEOREM. If z_c < z_min(close-packed surface layer) — here z_c ≈ 4.8 < 6–9 —
then removing a surface quantum's missing neighbors costs NO gross discount
(remaining patches still saturate the sphere): bond-loss surface energy is
confined to sites with z < z_c (edge/vertex class, measure ∝ A^{1/3}).
COROLLARY. The A^{2/3} surface term does NOT come from missing bonds. It lives in
the TAX column: the density edge forces a gradient localization tax (skin of
finite width; label: Weizsäcker gradient term — the shadow's own name, closing
the loop). Swing 1's sharp-limit a_s/a_v = 3/2 is thereby the unsaturated
overcount; capacity kills part of it — DIRECTION of the observed softening
(shadow 1.13 < 3/2) now derived; the coefficient needs the skin-profile
Euler–Lagrange (gradient tax vs capped contact profit) = swing-4 instrument.
Named honest bracket used below: a_s/a_v ∈ [1.13 (shadow), 1.5 (sharp limit)].

## 4. T-PEAK — registered before the location run (famous-number honesty in force)
Swing 1 proved existence/uniqueness: A* = 2(a_s/a_v)(a_v/a_c). Curve-free
a_c = 0.72 (swing 1). Bulk plateau datum a_v = 15.75 (named import: one bulk
number; the peak location is a DIFFERENT observable — ratio-forced cross-
prediction, the same discipline as N-body ratio tests).
    A* ∈ 2·[1.13, 1.5]·(15.75/0.72) = [49.4, 65.6]
REGISTERED: the experimental maximum of B/A (AME2020, experimental entries only)
falls in [49, 66]. The number A = 62 (Ni-62) is world-famous; the honesty
preamble (M-C1) already bars pretending ignorance — the claim is that the
DERIVED bracket contains it with no dial: both edges are forced quantities
(sharp-limit ratio 3/2; shadow ratio 1.13 named import), a_c is curve-free, and
the census bend can only push the true A* RIGHT within the bracket.
Falsifier: peak outside [49, 66] = the ratio bracket or a_c is wrong at the 20%
level; peak below 49 specifically indicts the capacity-softened a_s.
