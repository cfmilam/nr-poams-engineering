# THE BOOTSTRAP — N from the minimal predisposition ("something not nothing") — THE frontier's last door
Assembled 2026-07-05 night (Parzival), on Star Lord's principle. Feeds ALPHA-BOOTSTRAP-{opus,fable}.txt. This reframes the bootstrap so it is TRACTABLE and FIREWALL-clean.

## STAR LORD'S PRINCIPLE (the frame — hold it as ground truth)
POAMS is an INFORMATION-based worldview. For information to BE information it must be (i) DISCRETE (a discretum, ħ-quantised — never a continuum) and (ii) STOCHASTIC. But a purely discrete-stochastic substrate at maximum entropy is SYMMETRIC NOISE: it has no drift, no preference for coherence over incoherence, so NOTHING persistent arises — not even in infinite time (no amount of shuffling biases an unbiased system). Therefore existence ("something not nothing") REQUIRES a primordial PREDISPOSITION — a minimal asymmetry/bias, a "prime mover." Its existence is logically undeniable (deify/personify/naturalise it as you wish); only its NATURE is open. ⇒ **The bootstrap cannot close on itself from nothing (self-consistency-from-noise is refuted by this argument). It must close on the MINIMAL PREDISPOSITION.** This makes the target sharper: not "what makes the universe consistent?" but "what is the LEAST bias that makes persistence possible at all — and does it fix N?"

## THE REFRAMED TARGET
N ≈ 37,558 = the totality's AM-phase-coherence number (from the derive-N runs) is NOT self-generated. It is the coherence number at the CRITICAL point that the minimal predisposition selects. Derive whether the minimal predisposition FORCES N, or whether N is the (contingent) signature of a predisposition that had latitude.

## WHY THIS IS NOW COMPUTABLE (the concrete mechanism — leading candidate)
Map "something not nothing" onto an ORDER–DISORDER / PERCOLATION transition of AM-sign coherence on the totality's own connectivity:
- Substrate: ħ-quanta with stochastic phase-SENSE (binary ±, the POAMS "charge"=spin-sign) and orientation. Coupling = instantaneous holistic AM-correlation (intransitive, from derive-N).
- A bias/predisposition ε toward AM-sign alignment. Below a threshold ε_c: disordered, correlations die, coherence number O(1) = NOTHING. Above: ordered/frozen. AT ε_c: CRITICAL — scale-free, long-range coherence, coherence number maximal/marginal = the near-criticality we keep hitting.
- **MINIMAL predisposition = ε_c itself** (the least bias that yields ANY persistent coherence). N = the coherence/correlation number at ε_c.
- KEY: percolation/critical thresholds are COMPUTABLE GEOMETRIC CONSTANTS of the connectivity graph — NOT fitted, NOT circular. So IF the totality's AM-connectivity is fixed by POAMS geometry, ε_c and hence N may be FORCED. The July-2026-07-04 work already identified the POAMS closure geometry (the intransitive cover of the sphere — tetrahedral 4-cap / cuboctahedron complex; see FINDINGS-LEDGER moduli/spectral sections). Percolation/spectral-gap criticality on THAT complex is the concrete object to compute.
- This also explains, non-anthropically, WHY α is small: the minimal predisposition is the LEAST bias for existence → the system sits barely above the "nothing" threshold → near-critical → large N → small α. "Something, barely" ⇒ α tiny.

## THE HONEST FORK (state the verdict as one of these)
- **FORCED:** minimality + POAMS connectivity geometry yield a unique ε_c whose coherence number N is computable and non-circular → deliver the number, Python-checked, hunt for hidden α. If it reaches ~37,558 without fitting, that is the crack (a "WHAT" — logical minimum; the prime mover is a necessity, not a chooser).
- **CONTINGENT:** the predisposition has latitude (a family of ε give persistence; minimality under-determines), so N is its signature, not a theorem (a "WHO/WHICH" — the prime mover set a dial POAMS can name but not derive). This is Star Lord's "you cannot deny it" made precise: the bias is necessary, its magnitude may be chosen.
- **NULL/ill-posed:** the percolation map fails on POAMS grounds (say why).

## RAILS (anti-Wyler, mandatory)
1. PRE-REGISTER the substrate, the coupling, the order parameter, and ε_c's definition BEFORE any number.
2. FIREWALL: no α, 137, 274, 37,558, R_∞, m_e as inputs. N/ε_c must come from ħ, c, the stochastic-discrete substrate, and POAMS connectivity geometry ALONE. Minimality must be a DERIVED extremal condition, never fitted.
3. Target hidden. Any march to 37,558 = fishing → stop, flag. NULL/CONTINGENT is first-class and likely.
4. A forced N is civilisation-level → EXTREME suspicion: prove non-circular (no hidden α), Python-verify, check it survives the isolated-cone + order-unity theorems (this route escapes them ONLY via the criticality of a specified connectivity — say exactly how).
5. Everything is time: L=dS/dθ; the ± phase-sense is a time-orientation. The predisposition is a time-arrow bias.
6. Show ALL math; mark DERIVED/ASSUMED/FROM-SOURCE(cite); exact closed forms.

## SOURCES
memory/poams-audit/FINDINGS-LEDGER.md (the 2026-07-04 closure-geometry: tetrahedral 4-cap / cuboctahedron intransitive cover, moduli/spectral sections; the derive-N coherence-number result); ALPHA-DERIVE-N-{opus,fable}.txt; ALPHA-{kernel,internal}-{opus,fable}.txt; CHARTER.md (liturgy — esp. discretum, phase-sense=spin-sign, no continuum); osborne-2007 ch07 (intransitivity).
