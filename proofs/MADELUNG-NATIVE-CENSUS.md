# The Native Census — Closure Stacking Derives the Screening Ledger

**Madelung front, swings N-1/N-2 (the campaign's opening block).** Registration bcad068
(pre-execution; the b-value, the cell coefficient, and the neutrality identity hand-declared).
Companions: `MADELUNG-CLOSURE-THEOREM.md` (the families), `MADELUNG-FIXEDPOINT-THEOREM.md`
(the consolidated record; its open item 3 is the subject), `MADELUNG-DIP-ENCLOSURE.md` and
`MADELUNG-INTERIOR-REDUCTION.md` (the theorems that inherit tonight's chain). 2026-08-12.

**Scope and honesty.** The equation derived below is 98 years old — Thomas 1926, Fermi 1928;
March's reviews; and the rigorous statement that it is the Z → ∞ limit of the quantum atom,
Lieb & Simon 1977. No novelty of form is claimed anywhere in this document. The claim is the
**bookkeeping derivation**: that the census equation follows from the discreteum's own
counting rules — whole turns, phase-sense doubling, the zero-binding admission license, and
one range law — with **zero dials**. The falsifier risked at registration: if the forced
coefficient or the forced scale had landed anywhere but the classical values, the POAMS
reading would be dead. They land identically (§3).

## 1 · Swing N-1 — the cell theorem

**What is being counted.** A census entry is a *persistent* local organization of the one
substance about the eye: a closure. In a central ledger a closure carries three independent
winding counts — radial turns n_r (the in-and-out winding between apsides), polar l, azimuthal
m — the discreteum's own coordinates. (Extant shadow: Bohr–Sommerfeld quantization and Weyl
mode counting; tagged.)

**The cell is forced, not chosen.** Each independent winding direction accumulates action
∮p·dq; a *whole* turn is 2πħ of it, and a fractional turn is not a smaller entry but **no
entry** (GROUNDING §1: the discreteum). So distinct persistent modes sit one per action cell

$$(2\pi\hbar)^3,$$

and the extant "h³ per state" is the shadow of *only whole turns persist*. Each cell holds
**two** entries — the ± phase sense, the mode-doubling factor already derived on this front's
companion page (The Elements).

**Admission.** The census grows at the zero-binding frontier: an organization is admitted iff
its books close against the local ledger at E ≤ 0 — the same Normal-Realist license the
closure theorem uses as (H4). One license, two jobs: it admits the census here; it closes the
orbits that order the census there.

**The count.** At radius r the local rate budget is −V(r) (energy = trapped turning rate;
"potential" is the ledger's local deficit, an extant label tagged). The admissible windings
fill the action ball |p| ≤ p_F = √(2m(−V)), and the density of admitted entries is

$$n(r) \;=\; \frac{2}{(2\pi\hbar)^3}\cdot\frac{4\pi}{3}\,p_F^3
\;=\; \frac{\big(2m\,[-V(r)]\big)^{3/2}}{3\pi^2\hbar^3}.$$

**The 3/2 power and its coefficient are forced** — the power is the three independent winding
counts (a cube of a linear count), the coefficient is the whole-turn cell with its derived
sense doubling. Zero freedom. *(Named import: the independence/isotropy of the three winding
directions — the continuum measure of the count; extant anchor: Weyl counting.)*

## 2 · Swing N-2 — self-consistency: the census writes its own ledger

**Budget bookkeeping.** The local deficit is the eye's contribution plus the superposition of
the census's own entries under the **1/r range law — the one import**, and the same range law
that runs the Mass front's strain column (one import, two fronts). Sense assignment is
physical, not conventional: census members are mutually like-sense (their contact is strain);
the eye is opposite-sense (its term deepens the budget). "Attraction/repulsion" is sense
bookkeeping, not force (GROUNDING §3).

$$-V(r) \;=\; \frac{Ze^2}{r} \;-\; e^2\!\int \frac{n(r')}{|r-r'|}\,d^3r'
\qquad\Longleftrightarrow\qquad
\nabla^2(-V) = -4\pi e^2\, n \;\; (r > 0),\;\; -V \to \tfrac{Ze^2}{r} \text{ at the eye.}$$

**The census equation.** Write −V = Ze²χ(r)/r and insert the cell law:

$$\frac{Ze^2\chi''}{r} = 4\pi e^2\,\frac{(2m)^{3/2}}{3\pi^2\hbar^3}\Big(\frac{Ze^2\chi}{r}\Big)^{3/2}
\;\;\Longrightarrow\;\;
\chi'' = \frac{4}{3\pi}\Big(\frac{2m}{\hbar^2}\Big)^{3/2} e^3 Z^{1/2}\,\frac{\chi^{3/2}}{\sqrt{r}}.$$

Setting r = b·x and demanding unit coefficient:

$$b^{3/2} = \frac{3\pi}{4}\Big(\frac{\hbar^2}{2m}\Big)^{3/2}\frac{1}{e^3 Z^{1/2}}
\;\;\Longrightarrow\;\;
\boxed{\;b = \Big(\frac{3\pi}{4}\Big)^{2/3}\frac{a_0}{2\,Z^{1/3}} = 0.88534\,a_0\,Z^{-1/3}\;}
\qquad\Longrightarrow\qquad
\boxed{\;\chi'' = \chi^{3/2}/\sqrt{x}\;}$$

— identically the classical equation and the classical scale ((1/4)(9π²/2Z)^{1/3}a₀), with
nothing tuned. The hand-declared values at registration are hit exactly.

**Branch selection is whole-count bookkeeping.** The equation itself gives
√x·χ^{3/2} = xχ″, so the total census count is

$$N = \int n\,d^3r = Z\int_0^\infty \sqrt{x}\,\chi^{3/2}dx = Z\,\big[x\chi' - \chi\big]_0^\infty
= Z\big(\chi(0) + \lim_{x\to\infty} x\chi'\big).$$

χ(0) = 1 is the bare eye (the ledger's inner face is the eye's own 1/r). **Neutrality N = Z
forces xχ′ → 0: the Sommerfeld branch** — the decaying far field χ → 144/x³, i.e. exactly the
heteroclinic (0,0) → (3,12) whose dip and interior inequality were certified this week.
Finite-radius branches (χ hitting zero with slope) are the ions; the neutral census is unique
(Hille 1970, cited for rigor).

## 3 · What changed: the import audit

| item | before tonight | after |
|---|---|---|
| census equation χ″ = χ^{3/2}/√x | imported ("TF stands in") | **derived** (cell theorem + budget bookkeeping) |
| the 3/2 power | inside the import | **forced** (three whole-turn winding counts) |
| the coefficient / scale b = 0.88534 a₀Z^{−1/3} | inside the import | **forced** (whole-turn cell + derived ± doubling), zero dials |
| neutral branch (Sommerfeld) | boundary condition taken | **selected by whole-count bookkeeping** (N = Z) |
| 1/r range law | import (shared with Mass strain column) | import — **the one range law, now two fronts on one import** |
| three-winding independence (continuum measure) | inside the import | named import (Weyl-count class) |
| discrete → continuum smoothing | inside the import | named correspondence step (GROUNDING §1: continuity as far-field appearance); rigorous extant anchor Lieb–Simon 1977 |
| E = 0 admission | the (H4)-class license | unchanged — one license, two jobs (admission + closure) |

**Everything proved this week inherits the chain.** The dip certificate, the interior
inequality, the k_edge bracket [1.93405, 1.94133], the valve/tangency structure, and the
n+l families with their tie-break now ride: *discreteum cell + sense doubling + three
windings + E=0 license + one range law* — not "Thomas–Fermi imported."

## 4 · The correspondence clause and the correction tower (named, not claimed)

The census equation is the **far-field appearance of a discrete count** — many turns read at
once, the same correspondence step as L = dS/dθ being the macro representation of ΔS = ħΔn
(GROUNDING §1). Its discreteness corrections are therefore physics, not embarrassment, and
they are named as future fronts without claims: the eye-adjacent turns where the continuum
count fails first (extant shadow: the Scott correction class); and the same-sense pairing
layer (the Dirac-exchange class) — which is exactly the AXIOM-C front already open on the
Aufbau page. The d-collapse instrument already runs at that second tier.

## 5 · GROUNDING pass (the five questions)

1. *Real circulation named:* winding triples (n_r, l, m) about the eye; p/ħ is winding
   density, not a thing's speed. 2. *Bookkeeping, not force:* the budget is rate bookkeeping;
   attraction/repulsion enter only as sense assignments. 3. *Orbit vs spin:* these are
   orbital windings; the ± sense is the spin channel and enters only as the derived doubling.
   4. *Firewall:* zero dials; the coefficient and scale were the risked falsifiers, declared
   before execution. 5. *Convention vs channel:* the sense assignment (census like-sense,
   eye opposite) is physical channel structure — flipping it globally relabels, changes
   nothing; mixing it would change the sign of the strain term and destroy the census.
